<%@page import="boot.study.dto.LoginUserDto"%>
<%@page import="boot.study.service.LoginUserService"%>
<%@ page contentType="text/html; charset=UTF-8" %>
<%@ page import="java.util.*, java.sql.*" %>



<%
    String email = request.getParameter("email");
    String password = request.getParameter("password");
    String userType = request.getParameter("userType");

    LoginUserService loginUserService = new LoginUserService();
    LoginUserDto dto = userService.login(email, password, userType);

    if (user != null) {
        session.setAttribute("loggedInUser", user);
        if (userType.equals("personal")) {
            response.sendRedirect("pl_dashboard.jsp");
        } else {
            response.sendRedirect("cl_dashboard.jsp");
        }
    } else {
        response.sendRedirect("ml_login.jsp?error=invalidLogin");
    }
%>